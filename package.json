{
  "name": "video-app",
  "version": "1.0.0",
  "description": "Expose a catch-all endpoint that returns a video file at a certain path.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "serve": "node index.js & ./ngrok http 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "serve-index": "^1.9.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.21"
  }
}
